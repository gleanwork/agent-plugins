# Glean Agent Plugins

Consolidated Glean plugin repository for AI coding tools.

This repository keeps portable skills at the top level for `skills.sh` installs and uses `pluginpack` to generate the native Claude Code and Cursor plugin layouts without changing their published install surfaces.

```bash
npx skills add gleanwork/agent-plugins --skill search
npm run build
npm run test
```
